# ── 密钥（绝不入库）──────────────────────────────
.env
.env.*
!.env.example
*.key
.credentials.yaml

# ── 真实个人信息（绝不绝入库）────────────────────
# 真实简历样例：含姓名、电话、邮箱、照片。只在本地用作测试。
CV_example/
open_box/CV_example/
open_box/out/
out/
*.报告.json

# ── 一次性调查脚本 ──────────────────────────────
# 这两个脚本把某个真实简历的路径与姓名硬编码在里面，属于本地排查工具，
# 不进仓库（要复用请改成接收 argv 的通用版本）。
search-strategy/probe_mcm.py
search-strategy/probe_resume.py

# ── 会话记忆与系统文件 ──────────────────────────
node_modules
.workbuddy/
.DS_Store
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
