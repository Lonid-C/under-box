# ── 密钥（绝不入库）──────────────────────────────
.env
.env.*
!.env.example
*.key
.credentials.yaml

# ── 真实个人信息（绝不绝入库）────────────────────
# 真实简历样例：含姓名、电话、邮箱、照片。只在本地用作测试。
CV_example/
open_box/CV_example/
open_box/out/
out/
*.报告.json

# ── 一次性调查脚本 ──────────────────────────────
# 这两个脚本把某个真实简历的路径与姓名硬编码在里面，属于本地排查工具，
# 不进仓库（要复用请改成接收 argv 的通用版本）。
search-strategy/probe_mcm.py
search-strategy/probe_resume.py

# ── 会话记忆与系统文件 ──────────────────────────
node_modules
.workbuddy/
.DS_Store
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# ── 本地排查残留 ────────────────────────────────
# _recovery/ 是一次误覆盖后抢救出来的 .pyc 与半成品，只有本机排查价值；
# 本地运行.md 写死了这台机器的路径与 conda 习惯，且部分内容已被 start.sh 的修复取代。
_recovery/
本地运行.md
